import { openCloseMenuNav } from "./js/menu.js";

// add events click to treatments menu
// addEventsListener(menu__treatments, treatments__subMenu);
// addEventsListener(facials__subMenu, facialsList);
// addEventsListener(bodyTreatments__subMenu, bodyTreatmentsList);
// addEventsListener(regenerativeTreatment__subMenu, regenerativeTreatmentList);

// menu icon open/close

openCloseMenuNav();
